[package]
name = "mock-magicblock-vrf"
version = "0.1.0"
edition = "2021"
description = "Local-validator stand-in for MagicBlock ephemeral-vrf. Test harness only, never deployed."

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_magicblock_vrf"

[dependencies]
anchor-lang = "1.2.0"

# Its own workspace root on purpose: this crate must not perturb the audited
# program's workspace, lockfile, or release profile in any way.
[workspace]
